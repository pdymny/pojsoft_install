<!DOCTYPE html>
<html lang="pl">
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
	</head>
	<title>{{ title }}</title>
	<body style="font-family:firefly,DejaVu Sans,sans-serif;font-size:11px;">
		<br>
		<h2 style="text-align:center;text-transform:uppercase;margin-top:40px;">{{ title }}</h2>
		<table style="width:100%;margin-top:40px;">
			<tr>
			{% for head in headers %}
				<th>{{ head }}</th>
			{% endfor %}
			</tr>
			{% for tab in data %}
			<tr>
				{% for head in headers %}
					<td>{{ tab[head] }}</td>
				{% endfor %}
			</tr>
			{% endfor %}
		</table>
	</body>
</html>